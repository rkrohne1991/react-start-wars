import React from "react";
import { Fragment } from "react";

import classes from "./MainSection.module.scss";

const MainSection = () => {
  return (
    <Fragment>
      <main className={classes.mainSection}>MAIN SECTION</main>
    </Fragment>
  );
};

export default MainSection;
